# DL-05-003: Render sözleşmesi — statik 81 element üzerinde class diff

- Tarih: 2026-07-25
- Faz: 5 — Mimari Tasarım
- Mod: AUTOPILOT
- Karar: `index.html` başlangıçta 81 hücreyi statik olarak barındırır. `render(root, board, flags, solved)` DOM'u yeniden KURMAZ; yalnız değişen hücrelerde `classList.toggle` + `textContent` yazar (hedefli diff). `innerHTML` ile yeniden kurma kullanılmaz.
- Değerlendirilen alternatifler: (A) her güncellemede `innerHTML` ile tam yeniden render (D3'te Faz 4'te elenen alternatiflerden biri).
- Gerekçe: `innerHTML` yeniden kurma her tuş vuruşunda element kimliklerini ve odağı (`focus`) kaybettirir — bu NFR-4 (klavye erişilebilirliği, roving tabindex) ile doğrudan çelişir. Hedefli diff hem performans (NFR-1: ≤200ms, ölçülen <1ms) hem erişilebilirlik açısından üstün.
- Riskler: Diff mantığı hatalıysa bazı hücreler güncellenmeyebilir (stale UI) → Faz 9/11 testinde her state geçişinde tüm 81 hücrenin beklenen class/textContent'i doğrulanır.
- Geri alınabilirlik: Orta — render sözleşmesi (statik 81 element + diff) `app.js`/`render.js` sınırında sabit; değiştirmek HTML şablonunu da etkiler.
- İnsan onayı: Otomatik
- Varsayım mı?: Hayır
