# DL-11-001: Faz 11 — gerçek DOM/CSS sözleşme testi eklendi (jsdom'suz)

- Tarih: 2026-07-26
- Faz: 11 — Test
- Mod: AUTOPILOT
- Karar: `tests/contract.test.js` eklendi — `index.html`/`styles.css` GERÇEK içeriğini okuyup (a) 81 tekil `data-index`+`role="gridcell"`, (b) script yükleme sırası, (c) `#board`/`#status`/`#new-game` id'leri, (d) render.js'in `classList.toggle` ettiği `.given/.selected/.conflict` sınıflarının CSS'te tanımlı olduğunu doğruluyor. Faz 9'un `FakeElement` tabanlı testleri bu eşleşmeyi hiç kontrol etmiyordu (geliştiricinin varsayımı = test'in varsayımı).
- Değerlendirilen alternatifler: (1) jsdom eklemek — reddedildi (sıfır-bağımlılık ilkesi, DL-05 mimarisi); (2) hiçbir ek test yazmadan mevcut kapsamı yeterli saymak — reddedildi (kalıcı regresyon riski, calculator projesindeki aynı kör noktanın (Faz 10 F4) tekrarı olurdu).
- Gerekçe: Statik metin analizi bağımlılık eklemeden aynı riski (HTML/JS/CSS ayrışması) kalıcı olarak kilitliyor; `src/*.js` DEĞİŞTİRİLMEDİ (test-only, bulgu yok — kod zaten tutarlıydı).
- Riskler: Metin-tabanlı regex kontrolü gerçek tarayıcı DOM/CSSOM davranışını (ör. CSS önceliği, gerçek klavye olay modeli) taklit etmez; NFR-1/4'ün görsel/duyusal doğrulaması kullanıcı gözlemine bağlı kalır.
- Geri alınabilirlik: Yüksek (yalnız test dosyası + doküman, `src/` etkilenmedi).
- İnsan onayı: Otomatik
- Varsayım mı?: Hayır
- Kanıt: `npm test` → 48/48 pass; coverage %97,91 (tümü).
