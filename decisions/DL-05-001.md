# DL-05-001: Modül sınırları — 4 JS modülü, DOM'a yazan tek yer render.js

- Tarih: 2026-07-25
- Faz: 5 — Mimari Tasarım
- Mod: AUTOPILOT
- Karar: Kod dört modüle bölünür: `sudoku-core.js` (saf mantık, DOM'suz), `puzzles.js` (havuz verisi), `render.js` (DOM'a yazan TEK yer — class diff), `app.js` (controller/tek state sahibi). Bağımlılık yönü tek taraflı: `app → {core, render}`, `core → puzzles`. `core`/`puzzles` DOM'a hiç dokunmaz, bu yüzden Node'da doğrudan (jsdom'suz) test edilebilir.
- Değerlendirilen alternatifler: (A) tek dosya `app.js` içinde mantık+render+state birlikte.
- Gerekçe: Mantığın DOM'dan izolasyonu Faz 9/11'de `node --test` ile hızlı, deterministik birim testi sağlar (NFR-3: derleme/tarayıcı bağımlılığı yok). A alternatifi test için tarayıcı/jsdom gerektirir ve DOM ile mantığı karıştırıp regresyon riskini artırır.
- Riskler: Modüller arası sözleşme (public arayüzler) netleşmezse sınır ihlali olur → Faz 9 sözleşmesi bu DL'de imzalarla sabitlendi (bkz. mimari doküman "Public arayüzler").
- Geri alınabilirlik: Yüksek — dosya sınırları; birleştirme/ayırma sonradan kolay, public arayüz sabit kaldığı sürece çağıranlar etkilenmez.
- İnsan onayı: Otomatik
- Varsayım mı?: Hayır
