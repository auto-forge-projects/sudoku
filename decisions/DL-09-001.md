# DL-09-001: TDD ile 4 modül + statik yüzey + entegrasyon testi (TASK-001..006)

- Tarih: 2026-07-26
- Faz: 9 — Development
- Mod: AUTOPILOT
- Karar: Faz 5 mimarisindeki 4 saf JS modülü (sudoku-core, puzzles, render, app) + statik yüzey (index.html/styles.css) TDD (test→impl, ayrı commit) ile üretildi; entegrasyon testinde NFR-1 (10k `conflicts()` çağrısı 500ms bütçesinde, ölçülen ~155ms) ve NFR-2 (200 rastgele izomorfik dönüşümde `solve(applyTransform(givens,T)) === applyTransform(solution,T)` + tekillik) doğrulandı. Faz 7 güvenlik gereksinimleri (SEC-1/2/6/9/11) `tests/security.test.js` ile statik olarak zorlandı (innerHTML/eval/fetch/inline-script yasağı, CSP zorunluluğu).
- Değerlendirilen alternatifler: (a) NFR-2 için tüm havuzu (5 bulmaca) tek tek property test etmek yerine seeded PRNG ile 200 rastgele T üretip her turda havuzdan dönüşümlü örnekleme — sabit seed (deterministik, flaky değil) tercih edildi; (b) performans testi `Date.now()` yerine `process.hrtime.bigint()` — ms-altı çözünürlük ihtiyacı.
- Gerekçe: Mimaride (docs/05-architecture.md) tanımlı doğrulama yöntemleri (10k çağrı bütçesi, 200×T property test) birebir uygulandı; ayrı test-only solver (`tests/helpers/solver.js`) runtime'a sızmadan (SEC-9) tekillik/çözüm-korunumu kanıtı sağladı.
- Riskler: Perf bütçesi (500ms/10k) CI makinesine göre değişebilir — cömert tavan (ölçülen değerin ~3×'ü) bu riski azaltır; darboğaz olursa NFR-1 gerçek eşiği (200ms/tek-etkileşim) zaten çok daha gevşek.
- Geri alınabilirlik: Yüksek (kod + testler; mimariye aykırı bir seçim yok, geri alma = commit revert).
- İnsan onayı: Otomatik
- Varsayım mı?: Hayır
